### Dependencies ###

require(foreach) # Iterables
require(ggmcmc) # MCMC Plots
require(plyr) # Data tool
require(knitr) 
require(RCurl) # For google spreadsheets
require(manipulate)
require(doMC)
require(shiny)